{{ form_start(form, {'attr': {'id': 'blog_category-form'}}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
<div class="row">
    <div class="col-4 offset-8">
        <button class="btn btn-success" form="blog_category-form">{{ button_label|default('Save') }}</button>
        <a href="{{ path('blog_category_index') }}" class="btn btn-info">Back to list</a>
        {% if button_label is defined %}
            {{ include('blog_category/_delete_form.html.twig') }}
        {% endif %}
    </div>
</div>
